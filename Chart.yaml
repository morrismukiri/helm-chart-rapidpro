apiVersion: v2
name: rapidpro
description: Deploys RapidPro (v9) and, for dev/staging, its dependencies
icon: https://raw.githubusercontent.com/rapidpro/rapidpro/gh-pages/images/rapidpro.ico
type: application
# 2.0.0: v9-only redesign. Deployments + celery worker pools + beat singleton,
# hand-rolled dev/staging deps (no bitnami subcharts), gunicorn, non-root.
version: 2.0.0
appVersion: v9.0.0
maintainers:
- email: techops@ona.io
  name: Ona Engineering
